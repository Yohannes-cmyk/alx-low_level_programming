#include "holberton.h"

/**
 * mul - multiplication of to numbers
 * @num1: -integer one
 * @num2: - integer two
 * Return: 1 for digit, 0 for anything else
 */

int mul(int num1, int num2)
{
	int result;

	result = num1 * num2;
	return (result);
}
