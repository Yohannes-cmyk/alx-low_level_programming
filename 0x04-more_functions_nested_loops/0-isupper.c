#include "holberton.h"

/**
 * _isupper - is a function that checkes if the character is uppercase 
 * @a: is the character to be checked
 *Resurn 1 for success and 0 for failur.
 */

int _isupper(int a)
{
  	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
}
