#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 for success
 */


int main()
{
  int ch;
  for (ch = 'a';ch <= 'z'; ch++)
      putchar(ch);
  putchar(10);
  return(0);
}
